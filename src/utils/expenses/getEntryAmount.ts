// import { Account } from "types";
// import { createCreditAmount, createDebitAmount } from "../journals";

export default function getEntryAmount(
  amount: number,
  // account: Account,
  paymentAccountId: string
) {
  // const { accountType, accountId } = account;
  // if (accountId === paymentAccountId) {
  //   //this account should be credited
  //   return createCreditAmount(accountType, amount);
  // } else {
  //   //all other accounts should be debited
  //   return createDebitAmount(accountType, amount);
  // }
}
